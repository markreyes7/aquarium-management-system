[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "ams-cli"
version = "1.0.1"
dependencies = ["requests"]

[project.scripts]
ams = "ams_cli.main:main"
